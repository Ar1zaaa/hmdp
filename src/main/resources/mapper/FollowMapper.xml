<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.hmdp.mapper.FollowMapper">

    <select id="getIntersects" resultType="java.lang.String" parameterType="java.lang.Long">
        SELECT
            a.`follow_user_id`, b.`follow_user_id`
        FROM
            tb_follow a
        INNER JOIN tb_follow b ON a.follow_user_id = b.follow_user_id
        WHERE
            a.user_id = #{userId} AND b.user_id = #{targetId}
    </select>
</mapper>